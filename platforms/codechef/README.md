# cp-cli CodeChef client

HTTP client for the public CodeChef data used by [cp-cli](https://github.com/keys-i/cp-cli).

This crate exposes public profile and discussion data. It is an internal part of cp-cli,
but remains usable as a focused Rust HTTP client.
