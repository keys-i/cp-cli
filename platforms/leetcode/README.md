# cp-cli LeetCode client

HTTP client for LeetCode data and authenticated operations used by
[cp-cli](https://github.com/keys-i/cp-cli).

This crate exposes problems, account data, discussions, contests, testing, and submissions.
It is an internal part of cp-cli, but remains usable as a focused Rust HTTP client.
