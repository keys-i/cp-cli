# cp-cli HackerEarth client

HTTP client for public HackerEarth practice data used by [cp-cli](https://github.com/keys-i/cp-cli).

This crate exposes practice problems and their public metadata. It is an internal part of
cp-cli, but remains usable as a focused Rust HTTP client.
